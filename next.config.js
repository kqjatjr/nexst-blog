/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "www.notion.so",
      "lh3.googleusercontent.com",
    ],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
